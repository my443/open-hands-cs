// 1.0 Create the window
// 2.0 Create the List of prayer request items
// 3.0 Create title entry box
// 4.0 Create the prayer request entry box
// 5.0 Create the save button 									[DONE]
// 6.0 Create the add item button (Above the title box Entry) 	[DONE]
// 7.0 Create the delete item button. 							[DONE]
// 8.0 Create a settings button									
// 9.0 Place 2.0 - 8.0 on the window.
// 10.0 Show Items 2.0 - 8.0
			
	// Buttons Tutorial at http://www.mono-project.com/GtkSharp:_Buttons
	// Button saveButton = new Button();
	// saveButton.Add(win);
	// saveButton.Show ();			
	// Button addItemButton = new Button();	
	// Button settingButton = new Button();
	// Button button = new Button();


/*  Scratchpad items go here
      static void delete_event (object obj, DeleteEventArgs args)
        {
                Application.Quit();
        }
        
        
// MessageDialogue Example

	MessageDialog md = new MessageDialog (null, DialogFlags.Modal, MessageType.Info, ButtonsType.Ok, "MSG");
	md.Run ();
    md.Destroy();        

// Converting a string to a string array.

              
 * */

